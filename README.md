# the-ghost-ai-

A single static landing page (`index.html`) for "Ghost AI — Your Private AI
You Can Call or Text": *"Your Private AI. Call or Text Anytime."*

## What it claims

- "Dead Simple" — *"No accounts. No apps. Just talk. Your agent gets smarter
  every time you use it — and everything stays in your cloud."*
- A hero video (`hero-video.mp4`) and two product screenshots
  (`ghost-os-feature.png`, `how-ghost-os-works.png`) illustrating "Ghost OS."

## What is here

`index.html`, one video, two images, a favicon. No backend, no build step, no
API calls. Like `ghost-concierge`, this is marketing for Ghost OS / Ghost AI,
not an implementation of it — the phone/text logic it advertises lives
elsewhere (`ghost-ai` is the candidate: it has a `backend-api` with phone,
SMS and AI orchestrator services).
