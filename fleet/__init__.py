"""What is out there, and whether it is well.

A box writes its own row. Nothing else can: the serial comes off the machine,
the versions come off the filesystem, the health comes from greenboot. There is
no fleet server that decides these things on a box's behalf, which is why the
same code runs whether one box is enrolled or four hundred.
"""
